const styles = (theme) => ({
  background: {
    backgroundColor: theme.palette.primary.main,
    padding: 40,
    display: "flex",
    flexDicrection: "center",
    alignItems: "center",
    justifyContent: "center",
    minHeight: "100vh",
    textAlign: "center",
    flex: "1 0 auto",
  },
  signup: {
    maxWidth : 400
  },
  checkBox :{
    textAlign: "left",
    marginTop : 10,
    marginBottom : 10,
  }
});
export default styles;
